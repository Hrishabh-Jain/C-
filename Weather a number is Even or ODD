// Online C++ compiler to run C++ program online
#include <iostream>
using namespace std;
int main() {
    // Write C++ code here
  int n;
  cout<<"Enter a number = ";
  cin>>n;
  if(n%2 == 0)
  cout<< n << "= This is even number "<<endl;
  else
   cout<<n <<" = This is even number"<<endl;

    return 0;
}
